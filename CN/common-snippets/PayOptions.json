{
  "options": {
    "type": "fieldset",
    "label": "选项",
    "repeat": false,
    "fields": {
      "options.provider": {
        "type": "select",
        "label": "支付供应商",
        "options": {
          "stripe": {
            "label": "Stripe"
          },
          "paypal": {
            "label": "Paypal"
          },
          "payoneer": {
            "label": "Payoneer"
          }
        },
        "val": "stripe",
        "required": true
      },
      "options.type": {
        "type": "select",
        "label": "支付类型",
        "options": {
          "charge": {
            "label": "收费"
          },
          "transfer": {
            "label": "转账"
          }
        },
        "val": "charge",
        "required": true
      },
      "options.stripe.secret_key": {
        "type": "password",
        "label": "Stripe秘钥",
        "show_if": "options.provider=='stripe'",
        "required": true,
        "val": "",
        "width": "100%",
        "breakLine": true
      }
    }
  }
}