{
  "input": {
    "type": "fieldset",
    "label": "Consolidate Options",
    "repeat": true,
    "fields": {
      "table_id": {
        "type": "select",
        "label": "Select Table",
        "bind": "TABLES",
        "empty": "",
        "required": true
      },
      "group_column": {
        "type": "select",
        "label": "Group Column",
        "required": true,
        "table": "table_id",
        "bind": "COLUMNS"
      }
    }
  }
}