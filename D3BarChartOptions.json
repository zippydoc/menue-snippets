{
  "options_box": {
    "type": "fieldset",
    "label": "Options",
    "repeat": false,
    "fields": {
      "options.chart_type": {
        "type": "hidden",
        "val": "bar_chart"
      },
      "options.x_col": {
        "type": "select",
        "label": "X-axis column",
        "table": "table_id",
        "bind": "COLUMNS",
        "required": true
      },
      "options.value_col": {
        "type": "select",
        "label": "Value column",
        "table": "table_id",
        "bind": "COLUMNS",
        "required": true
      }
    }
  }
}