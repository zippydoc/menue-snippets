{
  "options": {
    "type": "fieldset",
    "Beschriftung": "Optionen auswählen",
    "repeat": false,
    "fields": {
      "task_type": {
        "type": "select",
        "Beschriftung": "Aufgaben-Typ",
        "required": true,
        "options": {
          "encrypt": {
            "Beschriftung": "Verschlüsseln (sicher)"
          },
          "decrypt": {
            "Beschriftung": "Entschlüsseln (sicher)"
          },
          "encrypt_simple": {
            "Beschriftung": "Verschlüsseln (einfach)"
          },
          "decrypt_simple": {
            "Beschriftung": "Entschlüsseln (einfach)"
          },
          "mask": {
            "Beschriftung": "Maske"
          }
        },
        "val": "encrypt"
      },
      "mask_type": {
        "type": "select",
        "Beschriftung": "Masken-Typ",
        "show_if": "task_type == 'mask'",
        "required": true,
        "options": {
          "trailing": {
            "Beschriftung": "Nachlaufende N-Zeichen"
          },
          "leading": {
            "Beschriftung": "Führende N-Zeichen"
          },
          "numbers": {
            "Beschriftung": "Zahlen"
          },
          "alphabets": {
            "Beschriftung": "Zeichen"
          },
          "regex": {
            "Beschriftung": "Regulärer Ausdruck"
          }
        },
        "val": "trailing"
      },
      "password": {
        "type": "password",
        "Beschriftung": "Kennwort",
        "show_if": "task_type != 'mask'",
        "required": true,
        "minLength": 1,
        "maxLength": 31,
        "val": ""
      },
      "varn": {
        "type": "number",
        "Beschriftung": "N",
        "show_if": "task_type == 'mask' && (mask_type == 'trailing' || mask_type == 'leading')",
        "required": true,
        "val": "1"
      },
      "regexp": {
        "type": "text",
        "Beschriftung": "Ausdruck",
        "show_if": "task_type == 'mask' && (mask_type == 'regex')",
        "required": true,
        "val": "",
        "placeholder": "[a-fA-F0-9]"
      },
      "replace": {
        "type": "text",
        "Beschriftung": "Maskenzeichenfolge",
        "show_if": "task_type == 'mask' && (mask_type == 'regex')",
        "required": true,
        "val": "*"
      }
    }
  }
}