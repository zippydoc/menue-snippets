{
  "options": {
    "type": "fieldset",
    "Etiqueta": "Seleccionar Opciones",
    "repeat": false,
    "fields": {
      "task_type": {
        "type": "select",
        "Etiqueta": "Tipo de Tarea",
        "required": true,
        "options": {
          "encrypt": {
            "Etiqueta": "Encriptar (seguro)"
          },
          "decrypt": {
            "Etiqueta": "Desencriptar (seguro)"
          },
          "encrypt_simple": {
            "Etiqueta": "Encriptar (simple)"
          },
          "decrypt_simple": {
            "Etiqueta": "Desencriptar (simple)"
          },
          "mask": {
            "Etiqueta": "Máscara"
          }
        },
        "val": "encrypt"
      },
      "mask_type": {
        "type": "select",
        "Etiqueta": "Tipo de Máscara",
        "show_if": "task_type == 'mask'",
        "required": true,
        "options": {
          "trailing": {
            "Etiqueta": "Primeros N Caracteres"
          },
          "leading": {
            "Etiqueta": "N Caracteres de Adelante"
          },
          "numbers": {
            "Etiqueta": "Números"
          },
          "alphabets": {
            "Etiqueta": "Caracteres"
          },
          "regex": {
            "Etiqueta": "Expresión Regular"
          }
        },
        "val": "trailing"
      },
      "password": {
        "type": "password",
        "Etiqueta": "Contraseña",
        "show_if": "task_type != 'mask'",
        "required": true,
        "minLength": 1,
        "maxLength": 31,
        "val": ""
      },
      "varn": {
        "type": "number",
        "Etiqueta": "N",
        "show_if": "task_type == 'mask' && (mask_type == 'trailing' || mask_type == 'leading')",
        "required": true,
        "val": "1"
      },
      "regexp": {
        "type": "text",
        "Etiqueta": "Expresión",
        "show_if": "task_type == 'mask' && (mask_type == 'regex')",
        "required": true,
        "val": "",
        "placeholder": "[a-fA-F0-9]"
      },
      "replace": {
        "type": "text",
        "Etiqueta": "Cadena de máscaras",
        "show_if": "task_type == 'mask' && (mask_type == 'regex')",
        "required": true,
        "val": "*"
      }
    }
  }
}