{
  "output_autorun_frame": {
    "type": "fieldset",
    "etiqueta": "Table Key",
    "show_if": "output.auto_run=='true'",
    "repeat": false,
    "fields": {
		"options.task_filter": {
        "type": "select",
        "etiqueta": "Key-Lock pair filter",
        "options": {
          "specific_tables": {
            "etiqueta": "Specify Task(s)"
          },
          "current_flow_tables": {
            "etiqueta": "Current Flow Tasks"
          },
          "flow_tables": {
            "etiqueta": "Specify Flow(s)"
          },
          "all_tables": {
            "etiqueta": "All Tasks"
          }
		 }
		},
      "output.autorun.tasks": {
        "type": "text",
        "etiqueta": "Autorun the following Tasks",
        "val": "",
        "placeholder": "';' separated list",
        "required": true,
        "style": "width: 100%"
      },
      "output.autorun.define_type": {
        "type": "select",
        "etiqueta": "Define type",
        "required": true,
        "options": {
          "manually": {
            "etiqueta": "Define manually"
          },
          "table_content": {
            "etiqueta": "Define by table content"
          }
        },
        "val": "manually"
      },
      "output.autorun.names": {
        "type": "fieldset",
        "etiqueta": "Define Key Pins",
        "show_if": "output.autorun.define_type=='manually'",
        "repeat": true,
        "fields": {
          "name": {
            "type": "text",
            "etiqueta": "Key Pin",
            "val": "",
            "required": true
          },
          "value": {
            "type": "text",
            "etiqueta": "Key Pin Value",
            "val": "",
            "required": true
          },
          "delete": {
            "type": "button",
            "etiqueta": "Apagar",
            "function": "DELETE_ROW"
          }
        }
      },
      "output.autorun.name_column": {
        "type": "select",
        "etiqueta": "Select Key Pin Column",
        "show_if": "output.autorun.define_type=='table_content'",
        "table": "input_table",
        "bind": "COLUMNS",
        "required": true
      },
      "output.autorun.value_column": {
        "type": "select",
        "etiqueta": "Select Key Pin Value Column",
        "show_if": "output.autorun.define_type=='table_content'",
        "table": "input_table",
        "bind": "COLUMNS",
        "required": true
      }
    }
  }
}