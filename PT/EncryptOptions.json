{
  "options": {
    "type": "fieldset",
    "etiqueta": "Opções de Seleção",
    "repeat": false,
    "fields": {
      "task_type": {
        "type": "select",
        "etiqueta": "Tipo de Tarefa",
        "required": true,
        "options": {
          "encrypt": {
            "etiqueta": "Codificar (seguro)"
          },
          "decrypt": {
            "etiqueta": "Descodificar (seguro)"
          },
          "encrypt_simple": {
            "etiqueta": "Codificar (simples)"
          },
          "decrypt_simple": {
            "etiqueta": "Descodificar (simples)"
          },
          "mask": {
            "etiqueta": "Máscara"
          }
        },
        "val": "encrypt"
      },
      "mask_type": {
        "type": "select",
        "etiqueta": "Tipo de Máscara",
        "show_if": "task_type == 'mask'",
        "required": true,
        "options": {
          "trailing": {
            "etiqueta": "Primeiros N caracteres"
          },
          "leading": {
            "etiqueta": "Primeiros N Caracteres"
          },
          "numbers": {
            "etiqueta": "Números"
          },
          "alphabets": {
            "etiqueta": "Caracteres"
          },
          "regex": {
            "etiqueta": "Expressão Regular"
          }
        },
        "val": "trailing"
      },
      "password": {
        "type": "password",
        "etiqueta": "Palavra-passe",
        "show_if": "task_type != 'mask'",
        "required": true,
        "minLength": 1,
        "maxLength": 31,
        "val": ""
      },
      "varn": {
        "type": "number",
        "etiqueta": "N",
        "show_if": "task_type == 'mask' && (mask_type == 'trailing' || mask_type == 'leading')",
        "required": true,
        "val": "1"
      },
      "regexp": {
        "type": "text",
        "etiqueta": "Expressão",
        "show_if": "task_type == 'mask' && (mask_type == 'regex')",
        "required": true,
        "val": "",
        "placeholder": "[a-fA-F0-9]"
      },
      "replace": {
        "type": "text",
        "etiqueta": "Máscara de string",
        "show_if": "task_type == 'mask' && (mask_type == 'regex')",
        "required": true,
        "val": "*"
      }
    }
  }
}