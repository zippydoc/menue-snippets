{
  "options": {
    "type": "fieldset",
    "label": "Select Transaction Options",
    "repeat": false,
    "fields": {
      "options.initial_date": {
        "type": "select",
        "label": "Initial Date",
        "table": "input_table",
        "bind": "COLUMNS",
        "required": true
      },
      "options.period": {
        "type": "select",
        "label": "Periodicy",
        "table": "input_table",
        "bind": "COLUMNS",
        "required": true
      },
      "options.final_date": {
        "type": "select",
        "label": "Final Date",
        "table": "input_table",
        "bind": "COLUMNS",
        "breakLine": true
      },
      "options.volume": {
        "type": "select",
        "label": "Amount",
        "table": "input_table",
        "bind": "COLUMNS"
      },
      "options.growth": {
        "type": "select",
        "label": "Growth Rate",
        "table": "input_table",
        "bind": "COLUMNS",
        "breakLine": true
      },
      "options.end_date": {
        "type": "date",
        "label": "End Date of Table",
        "val": "",
        "required": true,
        "style": "width: 300px"
      }
    }
  }
}